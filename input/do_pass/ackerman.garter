# https://en.wikipedia.org/wiki/Ackermann_function
def ackerman(m: Int, n: Int) -> Int:
	if m ==<Int> 0:
		n + 1
	else: if (m > 0) && (n ==<Int> 0):
		ackerman(m - 1, 1)
	else: 
		ackerman(m - 1, ackerman(m, n - 1))

def seqH<'a, 'b>(a: 'a, b: 'b) -> 'b: b 

def seq<'a, 'b>(a: 'a, b: 'b) -> 'b:
	seqH<'a, 'b>(print<'a>(a), b)

seq<Int, Int>(
seq<Int, Int>(
seq<Int, Int>(
seq<Int, Int>(
	ackerman(0,0),
	ackerman(1,1)
),
	ackerman(2,2)
),
	ackerman(3,3)
),
	ackerman(4,0)
): Int 
