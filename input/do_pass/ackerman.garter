# https://en.wikipedia.org/wiki/Ackermann_function
def ackerman(m, n):
	if m == 0:
		n + 1
	else: if (m > 0) && (n == 0):
		ackerman(m - 1, 1)
	else: 
		ackerman(m - 1, ackerman(m, n - 1))

def seqH(a, b): b 

def seq(a, b):
	seqH(print(a), b)

seq(
seq(
seq(
seq(
	ackerman(0,0),
	ackerman(1,1)
),
	ackerman(2,2)
),
	ackerman(3,3)
),
	ackerman(4,0)
)
